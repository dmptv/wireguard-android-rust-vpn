# Android / Gradle
*.iml
.gradle/
.idea/
.kotlin/
local.properties
build/
app/build/
captures/
.DS_Store

# Rust
rust/vpn-core/target/
rust/vpn-core/generated/

# jniLibs/ is deliberately NOT ignored — the prebuilt .so files are committed
# so a fresh clone can build the app immediately, without Rust/NDK installed.
