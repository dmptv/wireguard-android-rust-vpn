<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- WireguardVpnService is declared in :data's manifest — Android merges it
         in here automatically when building the final APK (manifest merger). -->

    <application
        android:name=".VpnClientApp"
        android:label="VpnClient"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links: vpnclient://selftest, vpnclient://connect —
                 handled by Navigation Compose inside NavHost (see MainActivity). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vpnclient" />
            </intent-filter>
        </activity>
    </application>
</manifest>
